<section class="studio-section studio-positioning" aria-labelledby="positioning-heading">
  <div class="studio-wrap">
    <p class="studio-section-label">02 / One accountable partner</p>
    <h2 id="positioning-heading" class="studio-display studio-positioning-line" data-positioning-line>
      <% "Strategy, code, campaigns and automation work better when one team owns how they connect.".split.each do |word| %>
        <span class="studio-positioning-word"><%= word %></span>
      <% end %>
    </h2>
  </div>
</section>
