<% content_for :title, "FAQs | Mwlabs" %>
<% content_for :meta_description, "Find answers to common questions about M&W Labs services, project timelines, startup and enterprise fit, and how to get started." %>
<% content_for :structured_data do %>
  <%= jsonld_tag faq_jsonld %>
<% end %>

<%= render "pages/shared/hero",
  eyebrow: "FAQs",
  title: "Frequently Asked Questions",
  description: "Answers to common questions about our services, process and how we work with clients." %>

<section class="bg-white py-14 sm:py-16">
  <div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-12">
    <div id="faq-accordion" data-accordion="collapse" class="space-y-4">
      <% SeoHelper::FAQS.each_with_index do |(question, answer), index| %>
        <div class="rounded-2xl border border-slate-200 bg-white">
          <h2 id="faq-heading-<%= index %>">
            <button type="button" class="flex w-full items-center justify-between gap-3 px-6 py-5 text-left text-sm font-extrabold text-slate-950" data-accordion-target="#faq-body-<%= index %>" aria-expanded="<%= index.zero? %>" aria-controls="faq-body-<%= index %>">
              <%= question %>
              <i class="fa-solid fa-chevron-down text-xs text-blue-600"></i>
            </button>
          </h2>
          <div id="faq-body-<%= index %>" class="<%= index.zero? ? "" : "hidden" %>" aria-labelledby="faq-heading-<%= index %>">
            <div class="border-t border-slate-100 px-6 py-5 text-sm font-semibold leading-7 text-slate-600"><%= answer %></div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</section>

<%= render "pages/shared/cta" %>
